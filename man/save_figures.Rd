% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_figures.R
\name{save_figures}
\alias{save_figures}
\title{Save a ggplot / patchwork object to one or more files}
\usage{
save_figures(
  plot,
  paths,
  width = NA,
  height = NA,
  units = "in",
  dpi = 300,
  overwrite = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{plot}{A ggplot or patchwork object.}

\item{paths}{Character vector. One or more output paths. Extension sets
the device. Mix formats freely: \code{c("Figs/pca.pdf", "Figs/pca.png")}}

\item{width}{Numeric. Plot width.}

\item{height}{Numeric. Plot height.}

\item{units}{Character. \code{"in"} (default), \code{"cm"}, or \code{"mm"}.}

\item{dpi}{Numeric. Resolution for raster formats (default 300).}

\item{overwrite}{Logical. If FALSE, skip paths whose file already exists.}

\item{verbose}{Logical. Emit a message for each file saved or skipped.}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Named list of output paths (invisibly), keyed by extension.
}
\description{
When \code{overwrite = FALSE} (the default), any output file that already
exists on disk is skipped - \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} is not called for that path.
Set \code{overwrite = TRUE} to force re-rendering regardless.
}
\details{
Supported extensions: \code{pdf}, \code{png}, \code{svg}, \code{eps}, \code{tiff}, \code{jpeg} / \code{jpg}.
}
\examples{
\dontrun{
# Single format
save_figures(p_pca, make_path("P01", "pca", "pdf", "Figs"), width = 8, height = 6)

# Multiple formats in one call
save_figures(
  p_pca,
  paths = make_path("P01", "pca", c("pdf", "png"), "Figs"),
  width = 8, height = 6
)
}

}
