% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_with_figures.R
\name{cache_with_figures}
\alias{cache_with_figures}
\title{Cache data AND save figures in one call}
\usage{
cache_with_figures(
  data_paths,
  figure_paths = NULL,
  expr,
  overwrite = FALSE,
  verbose = TRUE,
  width = NA,
  height = NA,
  units = "in",
  dpi = 300,
  csv_opts = list(),
  csv_read_args = list(),
  ...
)
}
\arguments{
\item{data_paths}{Character vector. Paths for the data cache.}

\item{figure_paths}{Character vector \emph{or} named list.
\itemize{
\item Plain vector: \code{result$plot} saved to all paths.
\item Named list: each name matches a key in \code{result$plots}; value is the
character vector of paths for that plot.
}}

\item{expr}{Unevaluated \code{{ block }} or \verb{function()} returning
\code{list(data = ..., plot = ...)}.}

\item{overwrite}{Logical. If TRUE recompute data and overwrite all files.
Figures are also re-rendered when TRUE.}

\item{verbose}{Logical.}

\item{width, height, units, dpi}{Forwarded to \code{\link[=save_figures]{save_figures()}}.}

\item{csv_opts, csv_read_args}{Forwarded to internal read/write helpers.}

\item{...}{Extra args forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The cached \verb{$data} object (invisibly).
}
\description{
On a cache miss the compute block is evaluated once. The block must return
a named list:
\itemize{
\item \verb{$data}: The object to cache (required).
\item \verb{$plot}: A single ggplot / patchwork saved to all \code{figure_paths}
(when \code{figure_paths} is a plain character vector).
\item \verb{$plots}: A \emph{named} list of plots (when \code{figure_paths} is a named list
keyed by plot name).
}
}
\details{
Figure caching follows \code{\link[=save_figures]{save_figures()}} - existing figure files are
skipped when \code{overwrite = FALSE}.

On a cache hit only the data is loaded. If you want to regenerate figures
on every run regardless, call \code{\link[=save_figures]{save_figures()}} separately with
\code{overwrite = TRUE}.

The compute block accepts both a bare \code{{ }} expression and a
\verb{function()} / \verb{\\()} lambda.
}
\examples{
\dontrun{
# Single plot
dat <- cache_with_figures(
  data_paths   = make_path("P01", "voom_clean", "rds", "DataClean"),
  figure_paths = make_path("P01", "pca", c("pdf", "png"), "Figs"),
  width = 8, height = 6,
  {
    obj <- build_voom(dat.raw)
    list(data = obj, plot = plot_pca(obj))
  }
)

# Multiple plots (named list of figure_paths)
dat <- cache_with_figures(
  data_paths   = make_path("P01", "voom_clean", "rds", "DataClean"),
  figure_paths = list(
    pca     = make_path("P01", "pca", c("pdf", "png"), "Figs"),
    heatmap = make_path("P01", "heatmap", "pdf", "Figs")
  ),
  width = 8, height = 6,
  {
    obj <- build_voom(dat.raw)
    list(
      data  = obj,
      plots = list(pca = plot_pca(obj), heatmap = plot_heatmap(obj))
    )
  }
)
}

}
