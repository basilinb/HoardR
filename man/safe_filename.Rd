% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_filename.R
\name{safe_filename}
\alias{safe_filename}
\title{Build a collision-safe file path}
\usage{
safe_filename(
  dir,
  basename,
  ext,
  overwrite = FALSE,
  version = c("none", "timestamp", "increment")
)
}
\arguments{
\item{dir}{Character. Target directory.}

\item{basename}{Character. Base name; any extension is stripped and
replaced by \code{ext}.}

\item{ext}{Character. Extension without leading dot (e.g. \code{"rds"}).}

\item{overwrite}{Logical. If TRUE return the plain path even if it exists.}

\item{version}{Character. Conflict strategy:
\itemize{
\item \code{"none"}: Append a timestamp only when a conflict is detected.
\item \code{"timestamp"}: Always append a timestamp.
\item \code{"increment"}: Append \verb{_v1}, \verb{_v2}, ... on each conflict.
}}
}
\value{
Character. Normalised path (the file is NOT created by this function).
}
\description{
Creates the parent directory if needed and returns a path that will not
silently overwrite an existing file (unless \code{overwrite = TRUE}).
}
