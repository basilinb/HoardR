% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_path.R
\name{make_path}
\alias{make_path}
\title{Build a consistent file path}
\usage{
make_path(project, analysis, ext, dir)
}
\arguments{
\item{project}{Character. Project identifier (e.g. \code{"P01"} or
\code{projectNumber_swab}).}

\item{analysis}{Character. Descriptive analysis label
(e.g. \code{"voom_clean"}, \code{"pca"}).}

\item{ext}{Character. One or more extensions without a leading dot
(e.g. \code{"rds"}, \code{c("csv","rds")}, \code{c("pdf","png")}).}

\item{dir}{Character. Target directory. Created if it does not exist.}
}
\value{
A named character vector of normalised paths, one per extension.
If only one extension is supplied the vector is unnamed.
}
\description{
The single source of truth for file naming across your project. Always
produces paths of the form \code{dir/project_analysis.ext}.
}
\details{
When \code{ext} is a vector, one path is returned per extension, named by
extension. This makes it easy to pass multiple formats to \code{\link[=cache]{cache()}},
\code{\link[=save_figures]{save_figures()}}, or \code{\link[=cache_with_figures]{cache_with_figures()}} in one call.
}
\examples{
\dontrun{
# Single path
make_path("P01", "voom_clean", "rds", "DataClean")

# Multiple extensions at once
make_path("P01", "results", c("csv", "rds"), "Results")
}

}
