% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cache}
\alias{cache}
\title{Cache any R object}
\usage{
cache(
  paths,
  expr,
  overwrite = FALSE,
  verbose = TRUE,
  csv_opts = list(),
  csv_read_args = list()
)
}
\arguments{
\item{paths}{Character vector. One or more output paths.}

\item{expr}{Unevaluated \code{{ block }} or \verb{function()} whose last expression
is the object to cache.}

\item{overwrite}{Logical. If TRUE always recompute and overwrite all paths.}

\item{verbose}{Logical. Emit messages on load / save.}

\item{csv_opts}{List. Extra args forwarded to \code{\link[readr:write_delim]{readr::write_csv()}} /
\code{\link[readr:write_delim]{readr::write_tsv()}}.}

\item{csv_read_args}{List. Extra args forwarded to \code{\link[readr:read_delim]{readr::read_csv()}} /
\code{\link[readr:read_delim]{readr::read_tsv()}}.}
}
\value{
The cached or freshly-computed object (invisibly).
}
\description{
Load if cached, otherwise compute and save. The extension of each path
determines the storage format:
}
\details{
\itemize{
\item \code{.rds}: Works for \emph{any} R object - DGEList, EList, list-of-lists,
model fits, data.frames, etc.
\item \code{.csv} / \code{.tsv}: Flat data.frames / tibbles \emph{only}. Passing a complex
object errors immediately with a clear message.
}

Multiple paths can be supplied; on a cache miss the object is written to
every format. On a cache hit the first existing file is read (preferring
\code{.rds} for round-trip fidelity).

The compute block is only evaluated on a cache miss and can be supplied as:
\itemize{
\item A bare \code{{ }} expression: \code{cache(path, { ... })}
\item A lambda / anonymous function: \code{cache(path, function() { ... })} or
\verb{cache(path, \\()\{ ... \})} (R >= 4.1)
}
}
\examples{
\dontrun{
# Cache a complex object
dat <- cache(
  make_path("P01", "voom_clean", "rds", "DataClean"),
  {
    # ... computation ...
    result
  }
)

# Flat table to csv + rds simultaneously
results_tbl <- cache(
  make_path("P01", "results", c("csv", "rds"), "Results"),
  { compute_results(dat) }
)
}

}
